#
# wofi config
#

#
# Run options
#
# mode can be drun (Desktop Run), run (from $PATH), or dmenu (for i/o)
mode=drun

# overlay allows for menu to display on top of fullscreen apps
layer=overlay

# placeholder text
#prompt=you phmell loik cheez
prompt=

# terminal to use for commands (default order is kitty, alacritty, etc.)
term=alacritty


#
# Search options
#
insensitive=true
filter_rate=100


#
# Visual options
#
width=800
height=400

# name of color file
#colors=colors

allow_images=true
allow_markup=true
no_actions=true

# keybindings
key_forward=Ctrl-n
key_backward=Ctrl-p
